# TaskHub 前后端启动与停用操作手册

> 适用项目：TaskHub（Spring Boot 3 后端 + Vue 3 / Vite 前端）
> 操作系统：macOS / Linux（命令以 zsh 为例，Windows 请参照对应说明）

---

## 1. 环境要求

| 组件     | 版本要求      | 校验命令            |
| -------- | ------------- | ------------------- |
| JDK      | 17            | `java -version`     |
| Maven    | 3.8+          | `mvn -v`            |
| Node.js  | 20.19+        | `node -v`           |
| npm      | 10+           | `npm -v`            |

---

## 2. 端口与访问入口一览

| 服务         | 默认端口 | 访问地址                              | 说明                          |
| ------------ | -------- | ------------------------------------- | ----------------------------- |
| 后端 API     | 8080     | http://localhost:8080                 | Spring Boot 内嵌 Tomcat       |
| Swagger UI   | 8080     | http://localhost:8080/swagger-ui.html | 仅 dev 环境开启，prod 关闭    |
| H2 控制台    | 8080     | http://localhost:8080/h2-console      | 仅 dev 环境开启               |
| 健康检查     | 8080     | http://localhost:8080/actuator/health | Actuator 端点                 |
| 前端(开发)   | 5173     | http://localhost:5173                 | Vite Dev Server               |
| 前端(预览)   | 4173     | http://localhost:4173                 | `npm run preview` 预览生产包  |

> 前端通过 Vite 代理把 `/api/**` 转发到后端（默认 `http://localhost:8080`），因此**必须先启动后端再启动前端**，否则页面接口会报代理错误。

---

## 3. 开发环境（dev）

### 3.1 启动后端

```bash
cd backend
mvn spring-boot:run
```

- 默认激活 `dev` profile，使用 H2 内存数据库，**重启后数据重置**。
- `app.seed-data=true`，启动时会自动写入演示数据。
- 启动成功标志：日志出现 `Started TaskHubApplication`，且 8080 端口可访问。

如需指定其他 profile：

```bash
mvn spring-boot:run -Dspring-boot.run.profiles=prod
```

### 3.2 启动前端

```bash
cd frontend
npm install      # 首次或依赖变更后执行
npm run dev
```

- 监听 5173 端口，浏览器访问 http://localhost:5173 。
- `vite.config.ts` 设置了 `strictPort: true`，**端口被占用会直接报错退出，不会自动换端口**（避免与代理配置错位）。

### 3.3 停用服务

**方式一：前台运行时**

在对应终端窗口按 `Ctrl + C` 即可优雅停止。后端配置了 `server.shutdown=graceful`，会等待正在处理的请求完成后再关闭。

**方式二：后台运行 / 找不到终端时，按端口反查并终止**

后端（8080）：

```bash
# 查出占用 8080 的进程 PID
lsof -nP -iTCP:8080 -sTCP:LISTEN
# 优雅终止（SIGTERM）
kill <PID>
# 若仍未退出，强制终止
kill -9 <PID>
```

前端（5173）——**注意**：5173 的实际持有者是 npm 外壳 fork 出的 vite 子进程（`node_modules/.bin/vite`），只杀 npm 外壳会留下孤儿进程继续占用端口：

```bash
# 1. 按端口反查真正持有监听套接字的进程
lsof -nP -iTCP:5173 -sTCP:LISTEN

# 2. 一并终止 npm 外壳与其 vite 子进程（先 SIGTERM）
kill <外壳PID> <vite子进程PID>

# 3. 逐个复核是否真正退出（无输出即已终止）
kill -0 <PID>

# 4. 若外壳忽略 SIGTERM（子进程已异常退出时可能发生），改用 SIGKILL
kill -9 <PID>
```

> 批量清理前务必确认各进程的父 PID 与工作目录（`ps -o pid,ppid,command -p <PID>`），避免误杀承载它的终端 shell，或其他项目中同名的 `npm run dev` 进程。

---

## 4. 生产环境（prod）

### 4.1 构建产物

后端打包（生成可执行 Jar，产物名固定为 `taskhub-backend.jar`）：

```bash
cd backend
mvn -ntp package
# 产物：backend/target/taskhub-backend.jar
```

前端生产构建：

```bash
cd frontend
npm ci            # 或 npm install
npm run build
# 产物：frontend/dist/（由 Nginx 等反向代理托管，并把 /api 转发到后端）
```

### 4.2 启动后端

```bash
cd backend
java -jar target/taskhub-backend.jar --spring.profiles.active=prod
```

生产环境特性：H2 文件模式持久化、关闭 Swagger 与 SQL 日志、`seed-data=false`、收敛跨域来源。

可通过环境变量注入敏感/差异化配置（占位符均带默认值）：

```bash
export APP_DB_URL=jdbc:h2:file:/data/taskhub
export APP_DB_USERNAME=sa
export APP_DB_PASSWORD=******
export APP_CORS_ALLOWED_ORIGINS=https://taskhub.example.com
export SERVER_PORT=8080

java -jar target/taskhub-backend.jar --spring.profiles.active=prod
```

后台常驻启动（日志重定向，nohup 方式）：

```bash
nohup java -jar target/taskhub-backend.jar --spring.profiles.active=prod \
  > logs/taskhub-backend.out 2>&1 &
echo $! > logs/taskhub-backend.pid   # 记录 PID 便于后续停止
```

> 提示：生产配置项（如 `SERVER_PORT`、`APP_SEED_DATA`）是否生效建议实际启动一次验证，不要仅凭文档推断。

### 4.3 停用后端

```bash
# 使用启动时记录的 PID
kill "$(cat logs/taskhub-backend.pid)"

# 或按端口反查
lsof -nP -iTCP:8080 -sTCP:LISTEN
kill <PID>          # 优雅停机，等待请求处理完毕
kill -9 <PID>       # 兜底强制终止
```

前端生产环境为静态资源，由 Nginx 托管，停用即停止/重载对应的 Nginx 站点，无需单独管理 Node 进程。

---

## 5. 常见问题排查

| 现象                                   | 原因与处理                                                                 |
| -------------------------------------- | -------------------------------------------------------------------------- |
| 前端启动报 `Port 5173 is already in use` | `strictPort` 生效，说明有残留进程。用 `lsof -nP -iTCP:5173 -sTCP:LISTEN` 反查并清理（含 vite 子进程）。 |
| 杀掉 npm 后端口仍被占用                | 只终止了外壳，vite 子进程成为孤儿。按 3.3 方式二连带子进程一起终止。          |
| 页面接口 404 / 代理错误                | 后端未启动或未监听 8080。先确认后端已启动，再刷新前端。                      |
| 后端 8080 端口被占用启动失败           | 反查占用进程并释放，或用 `SERVER_PORT` / `--server.port` 指定其他端口。       |
| Swagger / H2 控制台打不开              | prod 环境已关闭这两项，属正常行为；如需使用请切回 dev profile。              |

---

## 6. 快速命令速查

```bash
# —— 启动（开发）——
cd backend  && mvn spring-boot:run          # 后端 :8080
cd frontend && npm install && npm run dev   # 前端 :5173

# —— 停用（按端口）——
lsof -nP -iTCP:8080 -sTCP:LISTEN            # 查后端
lsof -nP -iTCP:5173 -sTCP:LISTEN            # 查前端（记得连带 vite 子进程）
kill <PID>                                  # 优雅停止
kill -9 <PID>                               # 强制停止

# —— 生产 ——
cd backend  && mvn -ntp package && java -jar target/taskhub-backend.jar --spring.profiles.active=prod
cd frontend && npm run build                # 产物 dist/ 交 Nginx 托管
```
